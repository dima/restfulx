from google.appengine.ext import db

class <%= class_name %>(db.Model):
<% for attribute in attributes -%>
  <%= attribute.name %> = db.<%= attribute.gae_type %>
<% end -%>
<% for model in belongs_tos -%>
  <%= model %> = db.ReferenceProperty(<%= model.camelcase %>)
<% end -%>
  
  